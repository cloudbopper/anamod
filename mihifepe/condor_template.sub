# Condor submit file template for feature importance subtask

executable                  = sh
arguments                   = run_worker.sh ARGS_FILENAME
universe                    = vanilla
rank                        = memory
request_memory              = 16 GB
requirements                = (OpSys == "LINUX" && arch == "X86_64")
log                         = LOG_FILENAME
output                      = OUTPUT_FILENAME
error                       = ERROR_FILENAME
getenv                      = True
queue
